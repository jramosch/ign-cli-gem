#!/usr/bin/env ruby

#this line ensures the computer/operating system knows this file needs ruby to run
#create metacritic file, which basically provides environment. gateway for user.

require './lib/metacritic'

Metacritic::CLI.new.call
